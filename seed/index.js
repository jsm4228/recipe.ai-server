const db = require("../db");
const { User } = require("../models");
// const chance = require('chance')

const main = async () => {};

const run = async () => {
  await main();
  await db.close();
};

run();
